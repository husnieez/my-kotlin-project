#!/usr/bin/env kotlin

fun main() {
    /*Long comment
     * w
     * w
     * package
     */
    //Create a variable for the number of inread messages.
    var count = 10
    println("You have $count unread messages")

    //Decrease the number of messages by 1
    count--
    println("You have $count unread messages")
}